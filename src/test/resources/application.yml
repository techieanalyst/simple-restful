server:
  servlet:
    context-path: /

datasource:
  main:
    driver: org.h2.Driver
    jdbc-url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
    username: sa
    password: sa